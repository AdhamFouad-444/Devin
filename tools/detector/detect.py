"""Detector stub — see contracts/README.md.

TODO(build): tail events/requests.jsonl + attacks.jsonl, classify incidents
(sqli/brute_force/scan/dos/cve_probe/anomaly), append events/incidents.jsonl
with status open -> triaged. Dedupe by (kind, source_ip) within 60s.
"""

"""Attack generator stub — see contracts/README.md for the full contract.

TODO(build): scenarios sqli|brute|scan|dos|cve against PORTAL_URL with
X-Sim-Source spoofed IPs + fake geo; append to events/attacks.jsonl;
--verify mode re-fires a probe and prints BLOCKED/SUCCESS.
"""
